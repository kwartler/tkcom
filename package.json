{
  "name": "tkcom",
  "private": true,
  "version": "0.1.0",
  "description": "OpenXcom browser port — Engine & Renderer Foundation",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
