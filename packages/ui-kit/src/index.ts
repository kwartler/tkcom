/**
 * @tkcom/ui-kit (Lane B)
 *
 * Shared DOM controls and game panels built with React, used by the editor
 * and management screens only (IMPLEMENTATION_PLAN.md Section 5.2). The
 * simulation never depends on this package. Vellum implements it.
 */

/** Package version marker until real components land. */
export const UI_KIT_VERSION = "0.1.0";
